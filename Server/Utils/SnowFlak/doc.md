雪花算法生成：
时间戳+机器ID+毫秒内序列号，

对比UUID、自增主键、雪花算法

缺点
自增主键：不适合分布式场景，
雪花算法：依赖时间，可能有回拨问题
UUID：完全无序随机（无意义），性能不高

优点：
自增主键：实现简单，单机首选
雪花：空间小性能高，顺序颁发，作为数据库主键性能高
UUID: 不依靠任何外部依赖，可靠

雪花回拨问题怎么解决：
1.10ms之内的小回拨
    时间不递增，使用序列号递增作为下一个ID
2.10ms以外的大回拨
    直接阻塞，直到真实时间追上逻辑时间